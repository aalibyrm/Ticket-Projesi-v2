spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    schemas: reporting_schema
    default-schema: reporting_schema
    locations: classpath:db/migration
  kafka:
    listener:
      auto-startup: false

app:
  security:
    jwt:
      enabled: false
