spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    schemas: ticket_schema
    default-schema: ticket_schema
    locations: classpath:db/migration

app:
  outbox:
    publisher:
      enabled: false
  security:
    jwt:
      enabled: false
